[build]
publish = "dist"
command = "pnpm build"
functions = "netlify/functions"
edge_functions = "netlify/edge-functions"

[dev]
command = "pnpm start"
publish = "dist"
framework = "qwik"

[functions]
node_bundler = "esbuild"